# Using Vite Proxy - No custom API URL needed
# Vite automatically proxies /api requests to localhost:3000
# This works with Cloudflare tunnel for frontend only

# If you need to override the proxy behavior, uncomment below:
# VITE_API_URL=http://localhost:3000
